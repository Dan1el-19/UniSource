---
"@unisource/sdk": minor
---

[stable] Add `/v2` subpath export. Move legacy v2 draft schemas to `@unisource/sdk/v2`. Freeze `UnisourceClient.v2` (no new methods). Drop `src` from npm files (dist-only).
